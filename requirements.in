# Reviewed direct dependencies. Security upgrade: 2026-09-05.
# FastAPI moves with Starlette; the provider SDK/httpx pair stays unchanged.
# Regenerate the universal Python 3.11+ lock with uv 0.11.33:
# uv pip compile --universal --python-version 3.11 --generate-hashes --no-strip-extras requirements.in -o requirements.lock
# Copy requirements.lock to requirements.txt. Docker installs with --require-hashes.
anthropic==0.40.0
fastapi==0.141.1
starlette==1.3.1
httpx==0.27.2
pypdf==6.16.1
python-multipart==0.0.31
uvicorn[standard]==0.27.0
Pillow==12.3.0
