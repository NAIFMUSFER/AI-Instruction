# تقرير تدقيق ACS — نسخة `AI3CLEAN.zip`

**التاريخ:** ١٥ أغسطس ٢٠٢٦ · **النطاق:** ٦١٩ ملفاً، ٢٣ م.ب · **الفرع المرجعي:** `remediation/production-trust`

كل رقم في هذا التقرير قياسٌ من تشغيل فعليّ داخل هذه النسخة، لا تقدير ولا استنتاج من قراءة.
كل ما تعذّر قياسه مكتوبٌ صراحةً بأنه **غير مُتحقَّق منه** بدل أن يُدَّعى.

---

## ١ · الخلاصة

المحرّك الداخلي سليم إلى حدٍّ لافت. بعد تشغيل كل حزمة قابلة للتشغيل بلا شبكة:

| الطبقة | النتيجة |
|---|---|
| حزم بايثون (٢١ ملفاً) | **٣٣٣٣ تأكيداً · صفر فشل** |
| حزم جافاسكربت (٥٩ ملفاً) | **٦٦٠٤ تأكيداً · صفر فشل** |
| تحقّق النشر `verify_deploy.sh` | **٥٨٦ تأكيداً · صفر فشل** |
| بوّابات البناء الخمس | كلّها تمرّ |
| **المجموع** | **≈ ١٠٥٠٠ تأكيد · صفر فشل** |

لكنّ الحزم لا تغطّي كل شيء. البحث عمّا **لا** تؤكّده أي حزمة أخرج **سبعة أعطال حقيقية**،
أُعيد إنتاج كلٍّ منها بالقياس ثم أُصلحت، وبندين تُركا مفتوحَين لأن إغلاقهما قرارٌ سلوكيّ لك.

أخطرها ثلاثة:

1. **صورة PNG بحجم ١٢٢ ك.ب تستهلك ٦٠١ م.ب وتُقبَل** — والنسخة على Render فئة `starter` تملك ٥١٢ م.ب.
2. **ملفّ PDF بحجم ٧٢ ك.ب يشغّل المعالج ٦٢ ثانية ويُقبَل** — والحدّ المسموح ١٢ م.ب، أي ١٦٦ ضعفاً منه.
3. **ستّ لوحات مشحونة كاملةً بلا أي مدخل** — مساحة العمل و BIM والتوثيق وجودة العرض
   والتفصيل المعماري ولوحة العرض: العلامات موجودة، والمنطق موجود، ولا سطر واحد في
   الشيفرة المشحونة يفتحها.

---

## ٢ · قبل كل شيء: الأرشيف وصل بنهايات أسطر CRLF

٣٦٦ ملفاً من الأرشيف بنهايات `\r\n` بينما المستودع نفسه LF. هذا ليس عطلاً في كودك،
لكنه يكسر النشر فعلياً:

* `sh tests/deploy/verify_deploy.sh` يفشل فوراً بـ `cd: can't cd to <root>` — الـ`\r`
  صار جزءاً من المسار. نفس الأمر ينطبق على `tools/netlify-build.sh` و`tools/vendor.sh`
  على وكيل بناء لينكس، وعلى سطور `RUN` في `Dockerfile`.
* `test_bundle_report.py` يفشل بعشرين تأكيداً: كل عدّ بايتات في `bundle_report.json`
  يخالف ما على القرص (`index.html` ‏٤٤٬٨٨٢ على القرص مقابل ٤٤٬٢٥٥ مُعلَناً).
  بعد التطبيع إلى LF طابقت الأحجام **بالضبط** وعاد الاختبار ٩١/٠.

**ما لم يتأثّر — قياساً لا افتراضاً:** بصمة خريطة الاستيراد في CSP سليمة. محلّل HTML
يطبّع `\r\n` إلى `\n` أثناء تهيئة مجرى الإدخال، فالمتصفّح يبصم صيغة LF. قِستُه في
Chromium حقيقيّ بالسياسة الإنتاجية كرأس استجابة فعليّ: صفر خرق، الخريطة مقبولة،
`textContent.length` = ١٣١ لا ١٣٦. كنت أتوقّع عطلاً هنا فلم يكن، والقياس هو ما منعني
من الإبلاغ عن شيء غير صحيح.

أُضيف `.gitattributes` حتى لا يعود هذا من سحب على ويندوز.

---

## ٣ · الأعطال المُصلَحة

### F-22 · صورة ١٢٢ ك.ب تستهلك ٦٠١ م.ب وتُقبَل — **حرج**

```
PNG 11000×3600 بلون صلب  →     ١٢٢٬٧٢٣ بايت على السلك   (الحدّ ٥٬٢٤٢٬٨٨٠)
                            ٣٩٬٦٠٠٬٠٠٠ بكسل             (الحدّ ٤٠٬٠٠٠٬٠٠٠)
                                ١١٬٠٠٠ أطول ضلع          (الحدّ ١٢٬٠٠٠)
validate_image(...)      →  ١٫٢٣ ثانية · ذروة ٦٠١ م.ب · **قُبِلت**
```

الحدود الثلاثة كلّها تقيس السلك أو عدد البكسلات؛ ولا واحد منها يقيس الرستر بعد فكّ
الترميز. وحارس قنبلة الانضغاط `Image.MAX_IMAGE_PIXELS` مضبوط **مساوياً** لسقف
البكسلات، فلا يمكن أن يعمل تحته أبداً. طلبٌ واحد غير موثّق كان يقتل العملية على نسخة
٥١٢ م.ب — ومعها تُمسح حالة حدّ الطلبات المحفوظة في الذاكرة. و`validate_images` تسمح بستّ.

**الإصلاح:** ميزانية `ACS_UPLOAD_MAX_IMAGE_DECODED_BYTES` (‏٣٢ م.ب) تُفحص **من الرأس قبل**
`im.load()`، ببايتات لكل بكسل مشتقّة من الوضع المُعلَن، مع ميزانية مشتركة للدفعة.
**بعد:** نفس الصورة تُرفض في ٠٫٠٠ ثانية بلا أي فكّ ترميز؛ مخطّط واقعيّ ‎4000×2800‎ ما زال يمرّ.

### F-23 · ملفّ PDF بحجم ٧٢ ك.ب يشغّل المعالج ٦٢ ثانية — **حرج**

صفحة واحدة، مجرى محتوى منضغط بنسبة ‎260:1‎:

| الملفّ | يتمدّد إلى | قبل | بعد |
|---|---|---|---|
| ٤ ك.ب | ٠٫٩ م.ب | ٠٫٣٧ ث | ٠٫٢٣ ث |
| ١٨ ك.ب | ٤٫٦ م.ب | ٥٫٠٤ ث | ٠٫٨٠ ث |
| **٧٢ ك.ب** | ١٨٫٤ م.ب | **٦٢٫٠٧ ث** | **٣٫١٢ ث** |
| ٢٦٨ ك.ب | ٧٤ م.ب | فوق الخطّي | **٠٫٠٣ ث — مرفوض** |
| ١٫٠ م.ب | ٢٩٤ م.ب | فوق الخطّي | **٠٫٠٤ ث — مرفوض** |

سقف الأربعمئة ألف حرف كان يُفحَص **بين** الصفحات ويُطبَّق **بعد** كلّ منها، لكن
`page.extract_text()` نفسها كانت بلا حدّ — و pypdf يفكّ المجرى ويحلّله إلى قائمة عمليات
**قبل** أن يبدأ الاستخراج، فسقف الأحرف لا يبلغ تلك المرحلة أصلاً.

**الإصلاح على طبقتين:** مراقب `visitor_text` يوقف الاستخراج فور بلوغ المساحة المتبقّية؛
و`_flate_expansion()` يقيس تمدّد كل مجرى بـ`max_length` صريح **قبل** أي استخراج، فلا
يبني القياس نفسه في الذاكرة أكثر من الميزانية. الحدّ ٢٤ م.ب، والرمز الجديد
`PDF_DECOMPRESSION_BOMB`.

### F-27 · ستّ لوحات مشحونة بلا مدخل — **مرتفع (فقدان وظائف)**

`#acsWorkspace` و`#rvPanel` و`#bxPanel` و`#dcPanel` و`#pqPanel` و`#adPanel` تشحن علاماتها
كاملةً في `public/index.html` ومنطقها كاملاً في `public/app/generated/*`، و**لا سطر واحد
في الشيفرة المشحونة يستدعي `init()` ولا `bind()` ولا `open()` لأيٍّ منها** — بحثٌ في
المستودع كلّه وجد الاستدعاءات داخل `tests/` وحدها. وكلّها `display:none` حتى يُضاف صنف
`.on` لا يضيفه أحد.

النتيجة: شجرة المشروع، والمفتّش، ومركز المسائل، وسجلّ المراجعات، والتراجع/الإعادة،
وتبديل اللغة، ولوحات BIM والتوثيق والعرض وجودة العرض والتفصيل المعماري — **غير قابلة
للوصول من المتصفّح**. ولأن `bind()` لم تُستدعَ قطّ، فاختصارات B/E/I/F و Ctrl+Z وحارس
`beforeunload` للعمل غير المحفوظ لم تُركَّب أيضاً: إغلاق التبويب كان يُلقي التعديلات بلا تنبيه.

**لماذا لم تكشفه أي حزمة:** كل حزمة قائمة تستدعي `init()` بنفسها قبل الفحص، فتُثبت أن
اللوحة تعمل **إن استُدعيت**، لا أن أحداً يستدعيها. الملفّ الوحيد الذي كان سيكشفه —
`tests/production/verify_live_browser.js`، وهو يبحث عن `#wsBtnTree` في الصفحة الحيّة —
لم يُشغَّل قطّ: `NOT VERIFIED — EXTERNAL ENVIRONMENT REQUIRED`.

**الإصلاح:** وحدة `public/app/ui/panels-entry.js` (يستوردها `main.js` أخيراً)، ومجموعة
أزرار في القشرة موصولة بـ`addEventListener` وحده (السياسة تمنع المعالِج داخل العلامة)،
و`window.ACS.exportModel()` مكشوفة من `workspace-ui-wiring.js` ليُسلَّم النموذج النشط للّوحات.

**الإثبات:** `tests/remediation/test_panel_entry.js` — ٢٩ تأكيداً في Chromium حقيقيّ تحت
السياسة الإنتاجية: اللوحات الستّ **تكتسب `.on` فعلاً**، و`init()` تركّب أزرار الشريط
العشرة فعلاً، والنقر بلا نموذج يشرح ما ينقص بدل فتح لوحة فارغة، وضابطٌ سالبٌ يؤكّد أنه
لا مدخل ثانٍ في الشجرة.

### F-19 · متغيّرات بيئة فارغة تمنع إقلاع الخادم — **مرتفع**

`int(os.environ.get("ACS_MAX_UPLOAD_MB", "12"))` مع المتغيّر `""` يرفع
`ValueError` — **عند استيراد الوحدة**، فلا تُحمَّل ولا يقلع الخادم. و`.env.example` نفسه
يشحن `ACS_MAX_BUILDING=` و`ACS_MAX_UPLOAD_MB=` و`ACS_REPAIR_ROUNDS=` **فارغة**، وترويسته
تقول `cp .env.example .env`.

حالة `ACS_REPAIR_ROUNDS` أسوأ من تعطّل الإقلاع: الخطأ يقع داخل عملية التوليد، فيُصنَّف
`classify_upstream`، ويصل المستخدم **502 «عطل غير مصنّف من مزوّد النموذج»** عن خطأ ضبط محلّي بحت.

### F-21 · `MAX_UPLOAD` مُعلَن في `/health` وغير مُطبَّق — **مرتفع**

بحثٌ في المستودع كلّه: موضعان فقط في الكود — تعريفه وعرضه في `/health` — ولا مقارنة واحدة.
وكلا مسارَي الرفع كان يقرأ الجسد كاملاً إلى الذاكرة **قبل** أي فحص حجم. الآن القراءة على
دفعات ٢٥٦ ك.ب وتتوقّف فور تجاوز الحدّ.

### F-20 · حقل `notes` بلا حدّ حجم

`/v1/edit` كان يفحص العدد (٤٠) ولا يفحص الحجم، والنصّ يُمرَّر إلى المُوجّه بـ`truncate=False`
فلا يقصّه `MAX_DESC_CHARS` أيضاً. أربعون ملاحظة بعشرة ملايين حرف = جسد ٤٠٠ م.ب مقبول.

### F-24 · `site_w=1e400` يظهر «عطلاً من مزوّد النموذج»

pydantic v2 يقبل `inf` افتراضياً. ثم `int(area / AREA_PER_ZONE[kind])` يرفع
`OverflowError` — وهو **ليس** ابناً لـ`ValueError`، فلا يلتقطه `except (TypeError, ValueError)`
المحيط به مباشرةً. يهرب إلى العملية الابنة، فيُصنَّف upstream، ويصل المستخدم **502** عن
حسابٍ محلّي — ويُسمَّم قياس أعطال المزوّد لديك، بلا أي تكلفة رموز على المهاجم.

### F-25 · الإنتاج يعمل بـ`ACS_ENV=development`

لا `render.yaml` ولا `Dockerfile` يضبطه. فيبقى `IS_PRODUCTION=False`، و
`STACK_TRACES = not IS_PRODUCTION` أي **`True` في الإنتاج**: أثر الاستدعاء كاملاً في
السجلّ عند كل عطل غير معالَج — وهو بالضبط ما ادّعى F-18 إغلاقه. وثانياً:
`/health` لا يرفع `PRODUCTION_WITHOUT_DISTRIBUTED_BACKEND`، فيُعلن حدّ الطلبات سليماً
بينما هو `MemoryBackend` داخل العملية — أي أن سقف `ACS_RL_GLOBAL_DAY=400`، الموصوف في
`render.yaml` بأنه «أهمّ صمّام أمان»، يُمسح مع كل نشر وكل إعادة تشغيل.

### F-26 · الحاوية تعمل بـuid 0 · F-28 · `?debug=1` بلا أثر · F-29 · `vendor.sh` مكسور

* لا `USER` في `Dockerfile`، و`python:3.11-slim` لا يحدّد مستخدماً.
* `debug-toggle.js` يمسح نمطاً سطرياً استبدلته F-09 بصنف — ومسحُ خاصيّة لا يهزم قاعدة صنف.
* `tools/vendor.sh` ما زال ينزّل `es-module-shims` **ويعدّه لازماً**، بينما
  `netlify-build.sh` يُفشل البناء إن وجد أثراً له. وينزّل ستّ إضافات والتطبيق يستورد
  **اثنتي عشرة**: ستّ إضافات المعالجة اللاحقة (SSAO، FXAA) ناقصة، فتسقط بصمت إلى
  `POST_UNAVAILABLE` بينما `verify-offline.mjs` يطبع PASS لأنه يفحص `ACS.ready` وحدها.

---

## ٤ · بندان مفتوحان — قرارٌ سلوكيّ لك

### KI-13 · `style-src 'self'` يُسقط سمات `style` التي تحقنها اللوحات

قِستُه في Chromium حقيقيّ أثناء اختبار F-27. سياسة CSP تمنع سمة `style` (وهذا ما تؤكّده
حزمتك `test_csp.js` أصلاً)، لكن تعليق `netlify.toml` استنتج أن الصفحة نظيفة لأنه فكّر في
`element.style.x = …` (CSSOM، وهو **مسموح**) وحده. الاستنتاج صحيح على القشرة الساكنة
وخاطئ على اللوحات.

الأثر الأوضح: في `generated/docs.js` يأخذ `.dc-sheet` **كلّ** هندسته
(`left/top/width/height/aspect-ratio`) من السمة المحجوبة، فينهار كل عرض لوحة إلى ‎0×0‎ في
الزاوية. ويفقد `acsErrorPanel` صفّ `display:flex` الحامل لزرّي «إعادة المحاولة» و«توليد محلّي».

**لم أُصلحه** لأن أكثر مواضع النداء داخل `public/app/generated/*` التي يولّدها
`tools/build_*_browser.py` من طبقات بايثون — فتحرير الجافاسكربت مباشرةً يُمحى عند أول
إعادة توليد. الإصلاح الصحيح في المولّدات، وهو سلوكيّ، ويجب إثبات أنه لا يغيّر سطح
`window.ACS`. لم أخلطه بالإصلاحات الميكانيكية في نفس الدفعة.

### KI-14 · التحقّق من الرفع والتخطيط يعملان على حلقة الأحداث

`validate_images` و`validate_pdf` و`json.dumps`×٢ و`EA.plan()` (‏O(n²)‏، ٦٠ تكراراً) نداءات
متزامنة تستهلك المعالج داخل معالِجات `async def` بلا `run_in_executor`. مسار النموذج
اللغوي مُبعَد بشكل صحيح عبر `run_job`؛ مسار التحقّق ليس كذلك. وuvicorn يملك حلقة واحدة،
فأي منها يجمّد كل اتصال آخر — بما فيه `/health`، فتعتبر Render النسخة غير سليمة وتعيد تشغيلها.

خفّفَت F-21/F-22/F-23 أسوأ حالة (من غير محدود إلى ≈٣ ثوانٍ)، لكن النقطة المعمارية قائمة.

---

## ٥ · ما تعذّر التحقّق منه هنا — وكيف تُغلقه

بيئة هذا التدقيق بلا وصول إلى PyPI ولا npm (‏403 على كليهما). لذلك:

| البند | الحالة | الأمر الذي يغلقه على جهاز متّصل |
|---|---|---|
| تشغيل الخادم فعلياً | **غير مُتحقَّق** — `fastapi` غير قابل للتركيب هنا | `pip install -r requirements.txt` ثم `uvicorn acs_understand_api:app` |
| القسم «د» من عقد الواجهة الخلفية | **غير مُتحقَّق** | `python3 tests/phase9_2/test_backend_contract.py` |
| العرض ثلاثي الأبعاد ببكسلات حقيقية | **غير مُتحقَّق** — لا three مُعبَّأ | `bash tools/vendor.sh && node tests/deploy/verify_page_boot.js` |
| مصفوفة حالات العرض A–J | **غير مُتحقَّق** | نفس الأمر أعلاه |
| Redis حقيقي لحدّ الطلبات | **غير مُتحقَّق** | `ACS_RATE_LIMIT_BACKEND=redis python3 tests/remediation/test_rate_limit.py` |
| سلوك Render مع `X-Forwarded-For` | **غير مُتحقَّق** — يحتاج مسباراً حيّاً | راجع §٦ |
| أرقام الرموز الحيّة (KI-6) | **غير مُتحقَّق** | `python3 tests/deploy/verify_backend_live.py --generation` |

**بند يستحقّ فحصاً حيّاً:** `acs_rate_limit.client_identity` مكتوبة بعناية حقيقية —
فهرسة من اليمين، `X-Real-IP` مطفأ افتراضياً، رفض الرؤوس المشوّهة، سقف ٣٢ قفزة. وهي صحيحة
**بشرط** أن تُلحق حافة Render دائماً عنوان النظير الحقيقي في آخر `X-Forwarded-For`. لم أجد
في المستودع أي اختبار أو خطوة تشغيل تقيس سلوك Render الفعلي هنا. إن مرّرت المنصّة يوماً
رأساً من العميل كما هو، صار `parts[-1]` تحت سيطرة المهاجم وسقطت كل حدود الزائر برأس واحد.

**ملاحظة على `VERIFICATION-RUNBOOK.md` سطر ٣٣٩:** الأمر
`for f in tests/phase1/*.js; do node tests/lib/run.js "$f"; done` قديم — `test_gate.js`
و`test_prov.js` يحتاجان DOM (يستعملان `__box`) فيفشلان في نطاق Node. يُشغَّلان عبر
`tests/phase3/lib/run_browser.js`، وهو يحتاج three مُعبَّأً.

---

## ٦ · ما فحصتُه ووجدتُه سليماً

هذا بقدر أهمّية ما سبق. لم أجد أي ثغرة في:

* **لا بدائيّات تنفيذ خطرة** في أي وحدة خادم: لا `eval` ولا `exec` ولا `pickle` ولا
  `os.system` ولا `shell=True` ولا `yaml.load`. النداء الفرعيّ الوحيد `["git", …]` بمصفوفة
  ثابتة بلا صدفة ومهلة ٥ ثوانٍ.
* **لا تسريب سرّ**: `/health` يعيد `api_key_configured` قيمةً منطقية؛ `_redis_url_facts`
  يصدّر المخطّط ووجود كلمة المرور فقط؛ `E.redact` يغطّي `sk-ant-*` والحاملات ويُطبَّق على
  كل رسالة صادرة. لا أثر استدعاء يصل العميل أبداً.
* **لا مسار اجتياز ولا ملفّ مؤقت**: `safe_filename_label` يُستعمل وسماً للسجلّ لا مساراً؛
  التحليل كلّه في `BytesIO`؛ `_save_raw` اشتراكيّ ويستعمل `O_EXCL|O_NOFOLLOW` بصلاحية ‎0600‎.
* **فحص أنواع الرفع بلا إعادة وسم صامتة**: التعارض بين المُعلَن والمشموم رفضٌ صريح؛ GIF
  وأي صورة متعدّدة الإطارات مرفوضة؛ البايتات المُمرَّرة معادة الترميز من البكسلات وحدها.
* **CORS قائمة سماح حقيقية**، واختيار النموذج من قائمة مسموحة، ولا سطح SSRF إطلاقاً.
* **تصميم الحدّ الموزّع**: السقف العام يُفحص بلا استهلاك ثم يُستهلك أخيراً — فزائرٌ مرفوض
  لا يُطفئ الخدمة للجميع؛ و`MemoryBackend` يحمل شواهد قبور تمنع إعادة تدوير المفاتيح.
  تصميمٌ مدروس فعلاً.
* **حارس JSON على `/v1/edit`** حقيقيّ: عمق ٤٠، مئة ألف مفتاح، ‎900 KB‎، ورفض
  `__proto__`/`constructor`/`prototype` في أي عمق.
* **الواجهة**: أصل واحد للـAPI متّسق في خمسة مواضع، ولا `eval` ولا `new Function` ولا
  `blob:` ولا عامل خارجيّ، و`fetch` واحد في التطبيق كلّه، ولا خطّ ولا صورة خارجيّة،
  وصفر معالِج داخل العلامات.

---

## ٧ · حالة التسليم

الأرشيف المرفق مستودع git بتاريخَين:

```
d48aca1  baseline: AI3CLEAN.zip كما وصل (بعد تطبيع CRLF إلى LF)
1b7f7c8  audit F-19…F-29: سبعة أعطال مقيسة مُصلَحة، بندان مفتوحان
```

فيمكنك قراءة `git show 1b7f7c8` أو `git diff d48aca1` لترى **بالضبط** ما تغيّر:
٢٠ ملفاً، ‎+١٥٩٨/−١٥٣‎ سطراً. لا ملفّ مُولَّد لُمس يدوياً؛ `bundle_report.json` أُعيد توليده
بأداته.

الاختبارات بعد الإصلاح:

| | قبل | بعد |
|---|---|---|
| `test_upload_security.py` | ١٩١ | **٢٠٠** (قسمان جديدان يثبّتان F-22 و F-23) |
| `test_build_metadata.py` | ٨٨ ناجحاً · ١ فاشل | **٩٣ · صفر** |
| `test_bundle_report.py` | ٧١ ناجحاً · ٢٠ فاشلاً (CRLF) | **٩١ · صفر** |
| `test_panel_entry.js` | — | **٢٩** (جديد، Chromium حقيقيّ) |
| `verify_deploy.sh` | لا يعمل (CRLF) | **٥٨٦ · صفر** |
| **المجموع** | — | **≈ ١٠٥٠٠ · صفر فشل** |
