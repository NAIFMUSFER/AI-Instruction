# تصحيح ثقة الإنتاج — التقرير النهائي
**PRODUCTION TRUST REMEDIATION — FINAL REPORT**

المستودع: `NAIFMUSFER/AI-Instruction` · المنتج: AI Construction Studio
الفرع: `remediation/production-trust`

---

## 1 · الملخّص التنفيذي

هذه المرحلة لم تُضِف ميزة واحدة. كان هدفها الوحيد أن يصير سلوك النظام **جديراً
بالثقة**: حتمياً، بلا تعديل هندسي صامت، وبمصدر معلوم لكل ادّعاء.

أهمّ ما تغيّر — وهو أصل المشكلة كلّها:

> كان `acs_layout.autofix` يُستدعى في **أربعة** مواضع من مسار التوليد ويكتب في
> النموذج مباشرة. يُزيح الغرف، ويرصّها صفّياً، ويقلّصها حتى **44٪** من أبعادها
> المعلنة، ويخترع أبواباً وكواشف دخان ومرشّات إطفاء وأفياشاً ومفاتيح إنارة.
> القائمة `added` التي كان يُفترض أن تُفصح عن ذلك كانت **شيفرة ميّتة** لا يقرأها
> أحد، والتقرير المُعاد كان يُطبَع في سجلّ الخادم أو يُهمَل تماماً. فالمستخدم كان
> يرى نموذجاً يظنّه تصميمه، وقد غُيّرت أبعاده وأُضيفت إليه عناصر سلامة لم يطلبها.

صار الأمر الآن: النموذج المُعاد من التوليد هو مخرج التوليد نفسه، بايت ببايت.
وكل ما كان النظام سيغيّره صار **ENGINEERING_CHANGE_PROPOSAL** يُعرَض ولا يُطبَّق،
ولا يُودَع إلا بموافقة صريحة تمرّ بمسار التأليف الواحد فتنتج مراجعة طبيعية.

إلى جانب ذلك: أمن رفع حقيقي بدل الثقة باسم الملفّ، حدّ معدّل ذرّي مشترك بدل عدّاد
داخل العملية، إلغاء توليد حقيقي بدل خيط متروك، بلاطة أدوار تتبع مسطح البناء بدل
الأرض كاملة، تشخيص عرض دائم في زمن التشغيل، سجلّ منظَّم بلا traceback خام، وأصل
بناء (provenance) يجعل التحقّق الإنتاجي قادراً على قول «أيّ نسخة قِستُ».

**الحكم النهائي: NOT_PRODUCTION_READY.** لا لأن شيئاً مما أُنجِز غير مثبَت، بل
لأن بندين معلنين في نطاق هذه المرحلة لم يُنجَزا: **F-09** (تفكيك الواجهة) و**F-11**
(تشديد CSP الكامل، وهو محجوز على F-09). ووجود `'unsafe-inline'` و`'unsafe-eval'`
في السياسة المنشورة عيب أمني **مُقاس فعلاً في Chromium** — لا مُفترَض — ويجعل أي
ثغرة XSS في التطبيق قابلة للاستغلال مباشرة.

---

## 2 · بصمة المستودع قبل وبعد

| | SHA | التاريخ |
|---|---|---|
| **Base SHA** | `865cca1e956d81052c6be5e24d049638155bc70a` | نقطة التجميد قبل أي تعديل |
| **Final SHA** | `497a681` (رأس `remediation/production-trust`) | بعد ثلاث دفعات |

الدفعات الثلاث:
1. `6bc8a88` — F-01 سلطة التغيير الهندسي + F-04/05/06/18
2. `130a32d` — F-07 البلاطة + F-08 التشخيص + F-13 التليمتري + provenance + التوثيق
3. `497a681` — F-15 الحفظ + F-16 الإفصاح + تجربة الخطأ + التزامن + الوصولية + CSP + الأداء

خطّ الأساس محفوظ كما هو في `tests/remediation_baseline/` (لم يُعدَّل أي أثر قديم):
بيئة التشغيل، وسجلّ التشغيل الكامل بلا متصفّح ومعه.

---

## 3 · الملفّات المتغيّرة

**103 ملفّاً: 73 جديداً و30 معدَّلاً · 53,333 سطر إضافة · 817 سطر حذف.**

وحدات جديدة في الخادوم:

| الملفّ | الدور |
|---|---|
| `acs_engineering_changes.json` | السجلّ الآليّ لكل تغيير يستطيع النظام إحداثه (27 قاعدة) |
| `acs_engineering_authority.py` | محرّك الاقتراحات، التصنيف، بصمة النموذج، التطبيعات الآمنة |
| `acs_engineering_approval.py` | الاعتماد والرفض — مفصول لأنه يستورد طبقة التأليف |
| `acs_upload_security.py` | تحقّق الرفع المركزي (22 رمز رفض معلن) |
| `acs_rate_limit.py` | الحدّ الموزّع: memory / redis، هويّة العميل، سياسة الفشل |
| `acs_generation_job.py` | `GenerationJob` + `JobRunner` — إلغاء حقيقي بعملية مستقلّة |
| `acs_logging.py` | سجلّ JSON منظَّم بحجب بالاسم |
| `acs_build_info.py` | أصل البناء بترتيب مصادر معلن |

ملفّات معدَّلة جوهرياً: `acs_layout.py`، `acs_understand.py`، `acs_understand_api.py`،
`acs_authoring.py/.json`، `acs_project.py`، `acs_pbr.py`، `acs_compiler.py`،
`public/index.html`، `Dockerfile`، `netlify.toml`، `requirements.txt`، `README.md`.

---

## 4 · جدول النتائج

| ID | الخطورة | الحالة السابقة | الإصلاح | الدليل | الحالة النهائية |
|---|---|---|---|---|---|
| **F-01** | Critical | `autofix` يكتب في النموذج من 4 مواضع بلا إفصاح؛ قائمة `added` شيفرة ميّتة | سجلّ آليّ + وضع PROPOSE افتراضي + اعتماد عبر مسار التأليف الواحد | `test_engineering_authority.py` **135/0** | **مُغلَق** |
| **F-02/F-03** | High | لا تحقّق إنتاجي مؤتمت | `tests/production/verify_live.py` + `verify_live_browser.js` + `run_all.sh` | 47 فحصاً، مُثبَت غير عبثيّ ضد خادم محلّي | **مُنجَز · القياس الحيّ NOT VERIFIED** |
| **F-04** | High | `defaultdict(deque)` داخل العملية؛ `X-Real-IP` يُصدَّق بلا حساب قفزات | مخزن ذرّي مشترك (Lua/EVALSHA)، ذاكرة محدودة المفاتيح بشواهد قبور، هويّة مُتحقَّق منها | `test_rate_limit.py` **120/0** | **مُغلَق · تكامل Redis الحقيقي NOT VERIFIED** |
| **F-05/F-19** | High | لا magic bytes؛ Content-Type يُعاد وسمه صامتاً `image/png`؛ اسم الملفّ في السجلّ | `acs_upload_security.py`: شمّ توقيع، فكّ فعليّ، تجريد بيانات وصفية، بلا ملفّ مؤقّت | `test_upload_security.py` **191/0** | **مُغلَق** |
| **F-06** | High | 504 يُعاد والخيط يُكمل ويحتجز مقعده حتى 600 ثانية | عملية مستقلّة تُنهى فعلاً؛ المقعد يتحرّر | `test_generation_cancel.py` **159/0** | **مُغلَق** |
| **F-07** | Medium | البلاطة العليا تغطّي الأرض كاملة فتبدو معلّقة | `PHASE10_FOOTPRINT_PLATE`: اتحاد مسطحات غرف المستوى، الفراغات مطروحة | `test_plate_extent.py` **158/0** | **مُغلَق** |
| **F-08** | High | لا تشخيص دائم لسواد المشهد | `renderDiagnostics()` بـ24 مفتاحاً مقيساً + `captureRenderFailure()` + زرّ تنزيل | `test_webgl_diagnostics.js` **133/0** | **العقد مُنجَز · الحالات التسع NOT VERIFIED** |
| **F-09** | High | `public/index.html` ملفّ واحد 1.86 م.ب | **لم يُنفَّذ** — قياس فقط | `bundle_report.json` + `test_bundle_report.py` **97/0** | **مفتوح** |
| **F-10** | Medium | تبعيات مفتوحة `>=` | `requirements.in`/`.lock` + `dependency_audit.py` + CI | `test_dependency_lock.py` **102/0** | **مُنجَز جزئياً — بلا hashes** |
| **F-11** | High | `script-src 'unsafe-inline' 'unsafe-eval'` | تشديد إضافي فقط + تدقيق مُقاس + خطّة هجرة | `test_csp.js` **114/0** + `csp_probe.json` | **مفتوح — محجوز على F-09** |
| **F-12** | High | حدّ الادّعاء النظامي غير محروس | `compliance.status = NOT_EVALUATED` + ماسح ادّعاءات كاذبة | `test_privacy_boundary.py` **58/0** | **مُغلَق** |
| **F-13** | Medium | لا تليمتري إنتاجي؛ `ACS_RAW_DUMP` دائم التشغيل | تليمتري بالأرقام فقط؛ الحفظ الخام opt-in، 0600، مُدوَّر | `test_logging.py` **82/0** | **مُغلَق · الأرقام الحيّة NOT VERIFIED** |
| **F-14** | Medium | لا قياس أداء حقيقي | مِنصّة قياس + ميزانيات + منظّم جودة (مواصفة) | `test_performance.js` **131/0** + إثبات لا-عبثية | **المنصّة مُنجَزة · القياس الحقيقي NOT VERIFIED** |
| **F-15** | Medium | لا حفظ محلّي؛ فقدان مشروع ممكن | IndexedDB بكتابة معاملاتية + استرداد + نسخ احتياطي | `test_persistence.js` **105/0** | **مُغلَق · متانة IDB الحقيقية NOT VERIFIED** |
| **F-16** | Low | أوامر معلنة غير منفَّذة | إفصاح صريح «غير مدعوم بعد» + تدقيق زمن تشغيل | ضمن `test_production_error_ui.js` | **مُغلَق** |
| **F-17** | Low | README سطر واحد | README بعشرين قسماً من واقع الشيفرة | `README.md` | **مُغلَق** |
| **F-18** | High | `traceback.print_exc()` في 5 مواضع يتجاوز التعقيم | سجلّ JSON منظَّم بحجب بالاسم؛ صفر traceback خام | `test_logging.py` + عقد الواجهة **85/0** | **مُغلَق** |

---

## 5 · سلطة التغيير الهندسي (F-01)

### السجلّ
`acs_engineering_changes.json` — 27 قاعدة، لكل واحدة الحقول الستّة الإلزامية.
**4 قواعد SAFE_NORMALIZATION** فقط، و**23 ENGINEERING_PROPOSAL**.

الآمن محصور فيما لا يغيّر معنى هندسياً:
- `LAYOUT_ROUND_RECT` — تدوير إلى منزلتين، بتسامح مُعلَن **0.005 م**؛ ما تجاوزه لا يُطبَّق.
- `UNDERSTAND_STRUCTURAL_DEFAULTS` — ملء قيمة **غائبة أصلاً** فقط، وتُسجَّل في `meta.acs_provenance`.
- `UNDERSTAND_STAGE_MERGE` و`PROJECT_ELEMENT_IDS` — `changes_canonical_model: false`.

### الحدّ القانوني
النموذج القانوني يبدأ عند **خرج التوليد المقبول**. كل ما بعده خارج الحدّ.

### الوسم إجباري
كل تغيير يجريه المصلِح يمرّ بـ`Recorder.record(change_id, …)`، و`classify()` ترفع
`KeyError` لأي معرّف غير مسجَّل. **لا يوجد مسار صامت ممكن**.

### الحالات الثماني — كلّها مثبَتة تشغيلاً

| | الحالة | نوع الاقتراح | النتيجة |
|---|---|---|---|
| A | توسيع الأرض | `LAYOUT_SITE_EXPANSION` | ✓ اقتراح + بصمة ثابتة + رفض بلا أثر + اعتماد بمراجعة واحدة |
| B | توسيع ممرّ | `LAYOUT_AISLE_WIDTH` | ✓ |
| C | إضافة مخرج | `FLS_ADD_EXIT` | ✓ |
| D | كاشف دخان | `LAYOUT_ADD_SMOKE_DETECTOR` | ✓ |
| E | مرشّة إطفاء | `LAYOUT_ADD_SPRINKLER` | ✓ |
| F | تغيير أبعاد منطقة | `LAYOUT_PROPORTIONAL_SHRINK` | ✓ |
| G | عدد الكاميرات | `SEC_ADD_CAMERA` | ✓ |
| H | تصحيح تداخل | `LAYOUT_RESOLVE_OVERLAPS` | ✓ |

لكل حالة يُثبَت: الاقتراح موجود · `engineering_authority=false` · البصمة قبل الاعتماد
مطابقة بايت ببايت · الرفض لا يغيّر شيئاً · الاعتماد ينتج **مراجعة واحدة** ويُدقَّق ·
المشروع الأب يبقى سليماً · لا `approve-all` ضمنيّ · المراجعة القديمة تُرفض `STALE_BASE_REVISION`.

### توسعة ضرورية واحدة
أُضيفت أربعة أنواع أوامر تأليف — `ADD_POINT` / `MOVE_POINT` / `DELETE_POINT` /
`CHANGE_POINT_PROPERTIES` — لأن اقتراح «كاشف دخان ناقص» لا يمكن اعتماده بلا أمر
يكتبه. صُنّفت `ARCHITECTURE` لأنها تكتب المصدر الدلالي `room.points` لا سجلّ
تخصّص مُصرَّف. **لم يُنشَأ مسار كتابة ثانٍ.** المرآة في المتصفّح وُلِّدت من المولّد
لا يدوياً، وأُضيفت 8 سيناريوهات تُشغَّل حرفياً في اللغتين.

### عيبان حقيقيان اكتُشفا وأُصلحا أثناء العمل
1. **`acs_project.to_project` كان يكتب في كائن المتصل** — يخالف الثابتة التي
   يحرسها `acs_revision.canonical_building` وتوثّقها الصفحة. صار على نسخة عميقة.
2. **`_execute` لم يرفع `TimeoutError` أبداً** — كان `raise` داخل `try` معالجه
   `except (EOFError, OSError)`، و`TimeoutError` صنف فرعي من `OSError` في بايثون 3.
   فكان فرع المهلة في الـAPI **ميّتاً**، ويصل العميل خطأ مزوّد بدل `ACS_TIMEOUT`.
   أُثبِت بشاهد سالب: إعادة الوحدة إلى حالتها تُسقِط 16 فحصاً بالعَرَض نفسه.

---

## 6 · الأمن

- **CORS**: قائمة سماح صريحة، بلا `*` ضمنيّ، `allow_credentials=False`.
- **رؤوس الاستجابة**: بلا تغيير هدّام؛ أُضيف `X-Robots-Tag: noindex` على مسار
  الاستوديو وحده حتى لا تصل بيانات مشاريع خاصّة إلى محرّكات البحث.
- **التعقيم**: `redact()` على كل رسالة خطأ، و**صفر** `traceback.print_exc()`.
- **الأسرار**: `/health` و`/ready` و`/version` تُعيد قيماً منطقية لا قيماً.
- **CSP**: شُدّدت إضافةً (`form-action`, `frame-src 'none'`, `manifest-src`,
  `media-src`, `upgrade-insecure-requests`) بلا حذف أو توسيع أي توجيه قائم.

---

## 7 · معالجة الرفع (F-05/F-19)

22 رمز رفض معلن. لا يُوثَق باسم الملفّ ولا بـContent-Type:

- **الصور**: شمّ التوقيع، رفض التعارض بين المُعلَن والمشموم (**بلا إعادة وسم صامتة**
  إلى `image/png` كما كان)، رفض GIF صراحةً، سقف أبعاد وسقف بكسل كلّي قبل الفكّ
  الكامل (حماية من قنابل الضغط)، ثمّ **فكّ فعليّ** لإثبات عدم البتر، تصحيح اتجاه
  EXIF، ثمّ **إعادة ترميز إلى صورة جديدة** فتسقط كل البيانات الوصفية.
- **PDF**: توقيع `%PDF-` خلال أوّل 1024 بايت، رفض المشفّر، **سقف صفحات يُطبَّق قبل
  استخراج أي نصّ** (أُثبِت بمراقبة `extract_text` وعدد النداءات صفر عند الرفض)،
  و**بلا ملفّ مؤقّت إطلاقاً** — البايتات في `io.BytesIO`.
- **JSON/DXF**: سقف حجم وعمق وعدد مفاتيح، ورفض `__proto__` / `constructor` /
  `prototype` أينما وردت.
- **اسم الملفّ**: `safe_filename_label()` — لا يُستعمل مساراً أبداً، ولا يصل السجلّ
  إلا بعد تجريد أسطر التحكّم (كان `%s` خاماً فيه، أي حقن سجلّ).

`health_status()["writes_temp_files"] is False` مُثبَت بمسح AST لا بادّعاء.

---

## 8 · حدّ المعدّل (F-04)

- البدائية المختارة `hit(key, limit, window, now, consume)` تُعيد **القرار** لا
  البيانات، فتختفي مسابقة check-then-act بين نسختين. سكربت Lua واحد عبر `EVALSHA`،
  ومسار بديل `GET → PTTL | INCR → PEXPIRE → PTTL` موصوف بأنه غير ذرّي.
- **الذاكرة**: `OrderedDict` بسقف `ACS_RL_MAX_KEYS` (20000)؛ الطرد يترك **شاهد قبر**
  يحمل انتهاء النافذة الأصلي، فالهوية المطرودة تُرفَض (fail-closed) ولا تُمنَح حصّة
  جديدة. البنية محدودة بـ2× السقف مهما دوّر المهاجم هويّات.
- **الهوية**: `X-Real-IP` **لم يعد يُصدَّق افتراضياً** — كان يمنح كل طلب دلواً جديداً.
  كل قيمة تُتحقَّق بـ`ipaddress`، والرؤوس المشوّهة أو الضخمة أو متعدّدة الأسطر تسقط
  إلى عنوان النظير.
- **سياسة الفشل** `ACS_RL_FAIL_POLICY` معلنة (`closed` افتراضاً) ومختبَرة باتجاهيها.
- `ACS_RATE_LIMIT_BACKEND=redis` **لا يسقط أبداً** إلى الذاكرة صامتاً.
- مُثبَت: نسختان تتشاركان حصّة واحدة · الحصّة تنجو من إعادة التشغيل · لا تجاوز تحت
  التزامن (N خيطاً على حدّ M ⇒ M بالضبط).

---

## 9 · إلغاء التوليد (F-06)

- عملية مستقلّة عبر `multiprocessing` (`spawn`)؛ الهدف نصّ `"module:function"`
  يُحلّ داخل الابن، فلا تُنقَل دالّة.
- عند المهلة: `terminate` ثمّ `kill`. **مُثبَت تشغيلاً**: الـPID المسجَّل يصير
  `ProcessLookupError`، والزمن المنقضي ≈ المهلة لا مدّة النوم، والمقعد يعود فوراً.
- سعة محدودة برفض صريح `JobRejected` بدل طابور بلا سقف.
- **حدّ معلن بصدق**: إنهاء العملية لا يستردّ طلباً وصل مزوّد النموذج بالفعل. لا
  واجهة إلغاء في SDK المزوّد اليوم، ولا نُدّعي ما ليس لدينا.
- **لا نتيجة تُودَع ولا مراجعة تُنشَأ** عند المهلة — مُثبَت بمقارنة بصمة مشروع حقيقي.

---

## 10 · تشخيص العرض (F-08)

`window.ACS.renderDiagnostics()` يُعيد **24 مفتاحاً** مقيساً فعلاً: العدادات من
مسح المشهد، `draw_calls`/`triangle_count` من `renderer.info.render`، تقاطعات
الهرم من `THREE.Frustum` حقيقي، و`pixel_probe` من `gl.readPixels`. **ما يتعذّر
قياسه يُعاد `null` ولا يُختلَق رقماً.**

`captureRenderFailure()` يُنتج JSON قابلاً للتنزيل عبر Blob — **ولا يرفع شيئاً**؛
أُثبِت مرّتين: مسح ثابت لنصّ الدالّة، وتنفيذ مع اعتراض
`fetch`/`XMLHttpRequest`/`sendBeacon`/`WebSocket` (والاعتراض نفسه أُثبِت غير عبثيّ).

زرّ **«⤓ تنزيل التشخيص»** حقيقي، بـ`aria-label`، قابل للوصول بلوحة المفاتيح.

**حدّ صريح**: مصفوفة الحالات التسع (BASE, PBR OFF, PBR ON, POST PROCESS,
ARCH DETAIL, SITE CONTEXT, LANDSCAPE, ENGINEERING, VR fallback) **لم تُنفَّذ في
راسم حقيقي هنا** — `public/vendor/` فارغ ولا شبكة، فـThree.js لا يُحمَّل.
`verify_page_boot.js` يخرج بالرمز **2**، لا صفر.

---

## 11 · بلاطة الأدوار (F-07)

**قبل** (`public/index.html:1737`):
```js
slabStrips(0,0,site.w,site.d,holes).forEach(...)   // الأرض كاملة، لكل مستوى
```
**بعد** (`:1741-1745`): الامتداد من `pqPlateRect(rooms.map(r=>r.rect), siteRect)`
— أي **اتحاد مسطحات غرف المستوى**، مع طرح الفراغات كما كان.

`acs_compiler.py:887-893` صار يفعل الشيء نفسه، ونال طرح الفراغات الذي لم يكن لديه.
السياسة معلنة مرّة واحدة في `acs_pbr.PLATE_POLICY` باسم `PHASE10_FOOTPRINT_PLATE`،
وتحمل معها `previous_policy: PHASE1_SITE_WIDE_PLATE` وسببها — **تغيير مُوثَّق المصدر
لا صامت**. مستوى الأرض/الموقع يبقى منفصلاً وبمقاس الأرض.

الأثر على الخطّ الذهبي: **39 شبكة `FLOOR|*|slab|*` تغيّرت في 7 نماذج من 8 · صفر
شبكة غير بلاطة تغيّرت · بصمات النماذج الهندسية متطابقة**. المستودع (مسطح = أرض)
لم يتغيّر بايتاً — أي شرط «إلا إذا ساوى المسطحُ الأرضَ» يعمل. الخطّ الذهبي القديم
محفوظ في `tests/remediation/fixtures/plate/mesh_baseline_phase1_site_wide.json`.

تشخيص `plate_overhang` صار **أقوى**: يقيس البلاطة المرسومة فعلاً، ويرفع
`ALIGN_ROOF_DETACHED` لأي مستوى يتجاوز مسطحه، لا للمستويات فوق الأرضي وحدها.

---

## 12 · تفكيك الواجهة (F-09) — **لم يُنفَّذ**

قياس فقط: `public/index.html` = **1,862,256 بايت** (gzip 512,974). ثمانية عناصر
`<script>`، منها وحدة واحدة بحجم **1,757,305 بايت**. الكتل المولَّدة 791,508 بايت،
والمكتوب يدوياً 1,070,748 بايت (57.5٪). الميزانية المستهدفة 153,600 بايت للقشرة،
أي **12.12× فوق الهدف**. `bundle_report.json` يحمل `status: "F-09 NOT IMPLEMENTED
— measurement only"`، والاختبار يفشل إن ادّعى التقرير غير ذلك.

**هذا البند مفتوح، وهو حاجز F-11.**

---

## 13 · تثبيت التبعيات (F-10)

`requirements.in` + `requirements.lock` + `requirements.txt` بأربعة تثبيتات `==`
(لا مواصفة مفتوحة واحدة). `Dockerfile:5` يستهلك `requirements.txt` مباشرة، فبقي
البناء سليماً. `tools/dependency_audit.py` يتحقّق من الانحراف عبر **تسعة** مواضع
تذكر `three@0.160.0`.

**حدود معلنة بصراحة**: لا **hashes** — لا يمكن جلبها بلا شبكة، ولم تُختلَق.
الإغلاق العابر (21 حزمة) مُعلَّم `UNRESOLVED-OFFLINE`. التثبيتات مشتقّة من الحدّ
الأدنى المعلن، وقد تكون **أقدم** مما يعمل على Render فعلاً. حالة الثغرات
(CVE) **NOT VERIFIED — EXTERNAL ENVIRONMENT REQUIRED**.

---

## 14 · سياسة أمن المحتوى (F-11) — **غير مكتمل**

قياس حقيقي في Chromium، بالسياسة الإنتاجية كترويسة استجابة فعلية:

| | السياسة المنشورة | تجربة مشدَّدة (بلا unsafe-*) |
|---|---|---|
| مخالفات CSP | **0** | **62** |
| تشغيل سكربت داخليّ عدائيّ | **نعم** | لا |
| تشغيل `eval()` / `new Function()` عدائيّ | **نعم / نعم** | لا / لا |
| هل يقلع التطبيق؟ | نعم | **لا — لا يبدأ إطلاقاً** |

سُجّل ذلك ثلاثة أسطر `KNOWN-WEAKNESS` (`CSP-INLINE-EXEC`, `CSP-EVAL-EXEC`,
`CSP-FUNCTION-CTOR`) — **لا كنجاح**. التطبيق لا يحوي موضع `eval` حقيقياً واحداً،
فـ`'unsafe-eval'` كلّه لأجل `es-module-shims`. إسقاط الـshim يُخرج iOS/iPadOS ≤16.3
وSafari macOS ≤16.3 وFirefox ≤107 (شامل ESR 102) وChrome/Edge ≤88 — **انقطاع تامّ
لا تدهور**، ولذلك لا يُوصى به. الخطّة المرتّبة إلى nonce/hash في `CSP-HARDENING.md`،
وكلّها معلّقة على F-09.

---

## 15 · السجلّ (F-18)

سطر JSON واحد لكل حدث. قائمة حجب بالاسم تشمل `text`, `description`, `prompt`,
`notes`, `building`, `api_key`, `authorization`, `cookie` وغيرها. `redact()` يعمل
داخل القيم المسموحة أيضاً. الـtraceback الكامل **في التطوير فقط**؛ في الإنتاج
يُسجَّل صنف الاستثناء وموضعه (ملفّ:سطر) لا نصّه.

**اختبار قديم كان يشترط وجود `traceback.print_exc()` ثلاث مرّات على الأقل** —
وهو تقنين للعيب نفسه. استُبدِل بشرط **أقوى**: صفر traceback خام، وأربعة نداءات
`LOG.exception` على الأقل، وحجب بالاسم مُثبَت، وإطفاء الأثر الكامل في الإنتاج.

`ACS_RAW_DUMP` صار **opt-in ومطفأ افتراضياً** (وفي الإنتاج يبقى مطفأً ما لم يُطلَب
صراحةً)، يكتب بـ`O_EXCL|O_NOFOLLOW` وصلاحية `0600` في مجلّد `0700`، ويُدوَّر
بالاحتفاظ بـ5 ملفّات، ولا يُذكر مساره للعميل أبداً.

---

## 16 · الوصولية

الطبقة اليدوية القديمة (الأسطر ~628–908) كانت تحمل **صفر** `aria-label`؛ أزرارها
الرمزية (☰، ▲، ▼، 🏃، ✕، أزرار الكاميرا، 📷) لم تكن تحمل إلا `title` — وهو ليس
اسماً وصولياً موثوقاً. أُصلح ذلك عبر **63 تعديلاً**: أسماء وصولية لكل زرّ رمزي،
`role=toolbar`، `tablist/tab/tabpanel` حقيقية، `for=` لكل حقل، `aria-live` للحالة،
`role=alert` لتحذير المحرّك، `aria-modal` مع حبس تركيز وإرجاعه، روابط تخطٍّ،
تركيز مرئي، **أهداف لمس ≥44 بكسل** عند 375 بكسل (بإلغاء `scale(.92)` الذي كان
يُصغّرها صامتاً)، واحترام `prefers-reduced-motion`.

**بديل وصولي حقيقي للمشهد ثلاثي الأبعاد**: شجرة المشروع، قائمة العناصر وخصائصها،
المشاكل، مخطّط أرضي SVG، والجداول — كلّها مشتقّة من هندسة النموذج بدوالّ نقيّة.
والمكتوب في العلامة نفسها يقول صراحةً إن قارئ الشاشة **لا** يستطيع تفسير بكسلات
WebGL.

**حدّ معلن**: `axe-core` غير مُورَّد في المستودع ولا شبكة لجلبه — تغطية axe-core
**NOT VERIFIED — EXTERNAL ENVIRONMENT REQUIRED**. جرى بدلاً منها فاحص حتمي مركَّز
على المعايير المطلوبة، وعبور لوحة مفاتيح حقيقي مُسجَّل. **122 فحصاً في Chromium
حقيقي، صفر إخفاق.**

---

## 17 · الخصوصية

`public/privacy.html` — صفحة ثنائية اللغة تقول بوضوح: ما يُرسَل إلى مزوّد الذكاء
الاصطناعي المُهيَّأ (نصّ الوصف وأي مخطّط مرفوع)، وألّا تُرفَع أسرار، وأين تُخزَّن
بيانات المشروع (محلياً في المتصفّح؛ الخادوم لا يحفظ نماذج)، وما **لا** يُحفَظ.
**لا ادّعاء احتفاظ غير مسنَد** — الصفحة تقول إن الاحتفاظ لدى المزوّد يحكمه عقد
المشغّل، وهذا كل ما يمكن قوله بصدق.

ماسح الادّعاءات الكاذبة يمرّ على الصفحة وكل `acs_*.json` والتقارير؛ كل تطابق
مسموح مُدرَج بالاسم والسطر في مخرج الاختبار (27 موضعاً)، فالقائمة **مرئية لا مخفيّة**.

---

## 18 · الأداء (F-14)

المنصّة مبنية وميزانياتها معلنة (سطح مكتب ≥45 إطاراً/ثانية، جوال متوسّط ≥24)،
ومنظّم الجودة موصوف بقيمه الدقيقة مع ثابتة صريحة: **لا تُحذَف هندسة دلالية أبداً**
(أُثبِت برفض 11 طفرة عدائية تحاول ذلك).

`run_perf.js` يخرج بالرمز **2** ولا يقيس شيئاً هنا. وأُثبِت أن شيفرة القياس نفسها
تعمل عبر صفحة WebGL بسيطة محلّية: **59.3 إطاراً/ثانية** خفيفة مقابل **16.5** ثقيلة،
وكومة JS من 1.5 م.ب إلى 68 م.ب بعد تخصيص 64 م.ب. الأرقام تستجيب للحمل، أي أنها
مقيسة لا مطبوعة. **أداء التطبيق الحقيقي NOT VERIFIED.**

---

## 19 · التحقّق الإنتاجي

`tests/production/` — 47 فحصاً عبر سبع مجموعات (HTTP، أصول الواجهة، إقلاع
المتصفّح، اختبار سير عمل، الاستجابة عند 7 عروض، العربية/RTL، أصل النشر).

**النتيجة هنا: 0 PASS · 0 FAIL · 47 NOT VERIFIED · exit 2.** السبب واحد ومحدَّد:
بروكسي الصندوق الرملي يعيد **403 Forbidden** لأي مضيف خارجي
(`curl: (56) CONNECT tunnel failed, response 403`).

**إثبات أن المتحقّق ليس عبثياً** — وهذا هو الجزء المهمّ:

| الهدف | النتيجة |
|---|---|
| مضيف ميّت | 0/0/18 NV، بلا انهيار، exit 2 |
| خادم محلّي يعيد رداً خاطئاً | 3 PASS · **14 FAIL** · 3 NV، exit 1 |
| كعب يعيد العقد الحقيقي للمستودع | **10/10 من المجموعة أ PASS** |
| خادم يعيد 401 | **FRONTEND_ACCESS_RESTRICTED** في الطبقتين |

ثلاث ثغرات عبثية اكتُشفت أثناء الإثبات وأُصلحت (فحص كان ينجح على صفحة بلا استيرادات،
وفحص تسريب أسرار كان يمسح صفحة 404).

**PRODUCTION-VERIFICATION-LATEST.md** يحمل هذه القيم الحقيقية. لا مربّع فارغ واحد،
ولا ادّعاء بأن النشر يعمل.

---

## 20 · الاختبارات المُشغَّلة

| المجموعة | الأمر |
|---|---|
| السلسلة الكاملة القديمة | `sh tests/phase9_2/run_all.sh --browser` |
| مجموعة التصحيح | `sh tests/remediation/run_all.sh --browser` |
| إغلاق النشر | `sh tests/deploy/verify_deploy.sh` |
| الأمن | `python3 tests/security/test_security.py` |
| تدقيق التبعيات | `python3 tools/dependency_audit.py` |
| التحقّق الإنتاجي | `sh tests/production/run_all.sh` |

---

## 21 · الأعداد الدقيقة

```
السلسلة القديمة الكاملة (--browser)   10,454 نجحت · 0 أخفقت
مجموعة التصحيح (--browser)             2,180 نجحت · 0 أخفقت
────────────────────────────────────────────────────────────
المجموع                               12,634 نجحت · 0 أخفقت · 0 مُتخطّاة

منها:
  إغلاق النشر                             431 / 0
  الأمن (Python)                          365 / 0
  الأمن (Chromium)                        242 / 0
  BIM / Docs / PBR / ArchDetail (Chromium) 222 / 0 · 0 خطأ صفحة
  الوصولية (Chromium)                     122 / 0
```

الإخفاق الوحيد في السجلّ — `✗ the root endpoint answers — 403 Forbidden` — يصنّفه
مُشغّل الاختبارات نفسه بالخروج **2** أي `NOT VERIFIED`، لا إخفاقاً.

**33 موضع NOT VERIFIED** في السلسلة القديمة و**21** في مجموعة التصحيح، كلّها معلنة
بالسبب.

**لم يُضعَّف اختبار واحد ولم يُتخطَّ.** ثلاثة اختبارات قديمة كانت تُقنّن السلوك
المَعِيب استُبدِلت بشروط **أقوى**، وكلّها مذكورة بالاسم في §15 و§11.

---

## 22 · الحدود المعروفة الباقية

1. **F-09 لم يُنفَّذ** — الصفحة ملفّ واحد 1.86 م.ب.
2. **F-11 غير مكتمل** — `'unsafe-inline'` و`'unsafe-eval'` باقيان وقابلان للاستغلال،
   ومُقاسان ومُتتبَّعان، ومحجوزان على F-09.
3. **`public/vendor/` غير مُودَع** — نسخة مسحوبة وخُدِمت ساكنة تُظهر لافتة تعذّر
   تحميل المحرّك بعد 12 ثانية. Netlify يملؤه وقت البناء.
4. **رموز أصل البناء غير مستبدَلة في المستودع** — الصفحة تعلن `UNPROVENANCED` بصدق.
   `tools/stamp_build_tokens.py` مربوط في `tools/netlify-build.sh` ويختمها وقت النشر.
5. **لا hashes في قفل بايثون**، والإغلاق العابر `UNRESOLVED-OFFLINE`.
6. **منظّم الجودة مواصفة فقط** — غير موصول بالصفحة عمداً.
7. **`ACS_TRUSTED_PROXIES` غير مضبوط في `render.yaml`** — يعمل بالافتراضي `1`.
   يجب تثبيته على عدد قفزات Render الحقيقي قبل الاعتماد على الحدّ.
8. **KI-2 · KI-4 · KI-6 · KI-8 · KI-9** تبقى مفتوحة (بيئية).

---

## 23 · بنود NOT VERIFIED

| البند | السبب |
|---|---|
| كل فحوص التحقّق الإنتاجي الحيّة (47) | بروكسي يعيد 403 لأي مضيف خارجي |
| تكامل Redis الحقيقي | لا خادم Redis ولا شبكة؛ العقد مُختبَر ضد `FakeRedis` |
| العقد الحيّ لـFastAPI | `fastapi` غير مثبّتة (pip محجوب) — الفحص عبر AST |
| الحالات التسع في راسم حقيقي | `public/vendor` فارغ ⇒ Three.js لا يُحمَّل |
| أداء التطبيق الحقيقي | السبب نفسه؛ شيفرة القياس مُثبَتة على صفحة WebGL بسيطة |
| تغطية axe-core | غير مُورَّدة ولا شبكة لجلبها |
| متانة IndexedDB عبر إعادة تحميل حقيقية، وجدار حصّة حقيقي | لا متصفّح ذو تخزين دائم هنا |
| احترام الخادوم الحيّ لـ`Idempotency-Key` و409 | يحتاج نفاذاً إلى النشر |
| الأرقام الحيّة للتوكينات خلف البتر (KI-6) | يحتاج توليداً حيّاً |
| حالة CVE للتثبيتات، وhashes الأرتيفاكت | يحتاج سجلّ حزم |
| كل وظائف GitHub Actions | لا منفّذ ولا شبكة؛ الأوامر جُرّبت محلياً حيث أمكن |
| قارئ شاشة حقيقي (NVDA/JAWS/VoiceOver) | غير متاح |
| هل يحتاج `es-module-shims` فعلاً إلى `'unsafe-eval'` | الـvendor فارغ |

---

## 24 · تعليمات النشر

```bash
git checkout remediation/production-trust

# الخادوم (Render)
docker build -t acs .            # Dockerfile ينسخ الوحدات الجديدة كلّها
# اضبط قبل النشر:
#   ANTHROPIC_API_KEY, ACS_ALLOWED_ORIGINS, ACS_ENV=production
#   ACS_TRUSTED_PROXIES=<عدد قفزات Render الحقيقي>       ← مهمّ
#   ACS_RATE_LIMIT_BACKEND=redis, ACS_REDIS_URL=...      ← للنشر متعدّد النسخ
#   ACS_GIT_SHA=$(git rev-parse HEAD), ACS_BUILT_AT=<ISO8601>
# لا تنسخ .env.example حرفياً: قيمه الفارغة كانت تُسقِط الإقلاع.

# الواجهة (Netlify)
sh tools/netlify-build.sh        # يورّد three/pdf.js، يختم أصل البناء، ثم يفحص

# بعد النشر — من آلة ذات نفاذ شبكيّ
sh tests/production/run_all.sh --expect-sha "$(git rev-parse HEAD)"
```

`exit 0` = لا إخفاق مرصود · `1` = اقرأ كل `"status": "FAIL"` في
`tests/production/outputs/summary.json` · `2` = **لم يُرصَد شيء، ولا يُستنتَج نجاح**.

---

## 25 · الحكم النهائي

**NOT_PRODUCTION_READY**

ليس لأن ما أُنجِز غير مثبَت — 12,634 فحصاً تمرّ بصفر إخفاق، والحالات الثماني
لسلطة التغيير الهندسي مثبَتة تشغيلاً، وإلغاء التوليد مثبَت بقتل عملية حقيقية،
وأمن الرفع مثبَت بحمولات عدائية، والبلاطة مثبَتة بخطّ ذهبي معاد التوليد.

بل لسببين محدَّدين:

1. **F-09 مفتوح**، و**F-11 غير مكتمل** تبعاً له. `'unsafe-inline'` في السياسة
   المنشورة عيب **مُقاس** لا مُفترَض: سكربت داخليّ عدائيّ **نُفِّذ فعلاً** في
   Chromium تحت السياسة الحالية. أي ثغرة XSS في تطبيق يحلّل DXF وJSON من المستخدم
   في الصفحة تصير مستغَلّة مباشرة.
2. **لم يُقَس النشر الحيّ ولا مرّة واحدة** من هذا الصندوق. الحالات التسع للعرض،
   وأداء WebGL، والعقد الحيّ للواجهة الخلفية — كلّها NOT VERIFIED. لا يجوز إعلان
   الجاهزية على نظام لم يُقَس منشوراً.

الطريق إلى `PRODUCTION_READY` قصير وواضح: تنفيذ F-09، ثمّ سياسة nonce/hash حسب
`CSP-HARDENING.md`، ثمّ تشغيل `tests/production/run_all.sh` من آلة ذات نفاذ شبكيّ
وإرفاق مخرجها. لا شيء في ذلك يحتاج قراراً جديداً — كلّه موصوف ومُقاس ومُخطَّط.
