# =============================================================================
# requirements-dev.txt — تبعيات الاختبار وحدها. لا تدخل صورة الإنتاج.
#
# لماذا ملفٌّ منفصل
# -----------------
# `requirements.txt` هو ما تنسخه الصورة وتثبّته (`COPY requirements.txt .`)،
# فكل سطرٍ فيه يُشحَن إلى الإنتاج. و`psutil` لا يستعمله الخادم في أي مسار —
# يستعمله اختبارٌ واحد ليتحقّق أن إلغاء مهمّة توليد لا يترك عملية يتيمة.
# وضعُه في requirements.txt يعني شحن مكتبةٍ إلى الإنتاج لأن اختباراً يحتاجها.
# لذلك: ملفٌّ ثانٍ، مثبَّت بنفس الصرامة، تُركِّبه وظائف CI التي تشغّل المجموعات.
#
# العطل الذي أوجب هذا الملفّ
# --------------------------
# الوظيفة `7 · Dependency audit and lock contract` تشغّل ٢٢ مجموعة اختبار
# على مفسّر Python **عارٍ**: خطواتها هي checkout ثم setup-python ثم
# setup-node ثم تشغيل المجموعات مباشرةً — لا `pip install` بينها إطلاقاً.
# (الوحيدة في تلك الوظيفة تأتي متأخّرةً في خطوة فحص الثغرات الاستشارية،
#  وتُركّب pip-audit وحده.)
#
# فكانت ستّ مجموعات تسقط لأن ما تستورده غير مركَّب أصلاً — أُعيد إنتاجه هنا
# بحجب الحزم نفسها عن المفسّر:
#     test_event_loop · test_upload_security · test_generation_cancel
#     test_privacy_boundary · test_api_wiring · test_p0_hardening
# وليس فيها عطلٌ منطقيّ واحد: بيئةٌ ناقصة لا شيفرةٌ خاطئة.
#
# الإصلاح في .github/workflows/ci.yml: تُركَّب التثبيتات قبل تشغيل المجموعات،
# كما تفعل الوظيفة ١ أصلاً. ولم تُجعَل أي تبعية «اختيارية» ولم يُلفّ استيرادٌ
# في try/except لتمرير اختبار: الحزمة مطلوبة فعلاً، فتُركَّب فعلاً.
#
# ⚠ الأرقام هنا مقيسة لا مشتقّة: 7.2.2 هو الإصدار الذي تعمل به هذه المجموعات
#   في البيئة التي مرّت فيها. وكما في requirements.lock، لا بصمات (hashes)
#   لأن بيئة التوليد بلا فهرس حزم. راجع DEPENDENCY-POLICY.md.
# =============================================================================

# tests/remediation/test_generation_cancel.py — يعدّ العمليات الأبناء بعد
# الإلغاء ليثبت أن إنهاء العامل لا يترك عملية يتيمة. لا يستعمله الخادم.
psutil==7.2.2
